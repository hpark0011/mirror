/**
 * Vertical divider component for project filter trigger.
 *
 * Creates a thin vertical line separator between filter sections.
 */
export function ProjectFilterDivider() {
  return <div className="w-px self-stretch mx-0 bg-border-light" />;
}
