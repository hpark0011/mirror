// Children-slot stub. The `[username]` route uses parallel slots, and the
// real new-article page renders under `@content/articles/new/page.tsx`.
// Mirrors `articles/[slug]/edit/page.tsx`.
export default function NewArticlePage() {
  return null;
}
