// Children-slot stub. The `[username]` route uses parallel slots, and the
// real edit page renders under `@content/articles/[slug]/edit/page.tsx`.
// Mirrors the sibling stub at `[username]/articles/[slug]/page.tsx`.
export default function ArticleEditPage() {
  return null;
}
