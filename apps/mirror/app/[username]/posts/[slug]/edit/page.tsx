// Children-slot stub. The `[username]` route uses parallel slots, and the
// real edit page renders under `@content/posts/[slug]/edit/page.tsx`.
// Mirrors the sibling stub at `[username]/posts/[slug]/page.tsx`.
export default function PostEditPage() {
  return null;
}
