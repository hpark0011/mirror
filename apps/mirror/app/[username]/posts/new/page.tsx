// Children-slot stub. The `[username]` route uses parallel slots, and the
// real new-post page renders under `@content/posts/new/page.tsx`.
// Mirrors `articles/new/page.tsx`.
export default function NewPostPage() {
  return null;
}
